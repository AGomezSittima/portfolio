---
interface Props {
  id: string;
  class?: string;
}

const { id, class: className } = Astro.props;
---

<section
  id={id}
  class:list={[
    "mx-auto flex flex-col justify-center mb-20 md:max-w-lg lg:max-w-2xl scroll-m-24",
    className,
  ]}
>
  <slot />
</section>
