---
import type { NavItem } from "@/lib/types";
import { Button } from "./ui/button";

const { label, href } = Astro.props as NavItem;
---

<Button asChild>
  <a
    class="hover:text-primary transition-all duration-200 hover:-translate-y-0.5"
    href={href}
  >
    {label}
  </a>
</Button>
